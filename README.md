# PracticasBD

Basic commands
```sh
$ git status
$ git add <.| -a>
$ git commit -m "Description message"
$ git push
$ git clone <repo_name>

```
Branch commands
```sh
$ git checkout -b <branch_name>
$ git add <.| -a>
$ git commit -m "Description message"
$ git push

```